const submit = document.querySelector("#login register");
if (submit) {
    submit.addEventListener("onsubmit", (e) => {
        e.preventDefault();
        console.log("teste");
    });
}
//# sourceMappingURL=index.js.map